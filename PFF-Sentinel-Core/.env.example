# PFF Sentinel Protocol — Environment Variables
# Copy this file to .env and fill in your actual values

# Supabase Configuration (Master Backend — must match PFF and SOVRYN CHAIN exactly)
# Use the same project URL and anon key across all three for Sentinel bonding.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Rootstock (RSK) Configuration
VITE_RSK_NETWORK=testnet
# Options: 'testnet' or 'mainnet'

# Token Contract Addresses (deploy your contracts first)
VITE_VIDA_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000
VITE_DLLR_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000
VITE_USDT_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# RSL Backend (Remote Sovereign Lock)
RSL_WS_URL=wss://your-lifeos-backend.netlify.app/.netlify/functions/rsl-ws
RSL_POLL_URL=https://your-lifeos-backend.netlify.app/.netlify/functions/rsl-poll

# Unified Security Channels: SOVRYN Audit API (Channels 2 + 3)
# Set to your Netlify site URL (e.g. https://your-site.netlify.app) so /v1/sovryn/* hits Netlify Functions.
# In Netlify: Site → Environment variables → add SOVRYN_SECRET (same value as below).
VITE_SOVRYN_API_URL=https://your-site.netlify.app
VITE_SOVRYN_SECRET=your-sovryn-secret-here

# Channel 4: Geographic & device pinning (expected country e.g. NG)
VITE_EXPECTED_COUNTRY=NG
# Optional: URL that returns { country_code, is_vpn } for current IP (used for VPN/geo check)
# VITE_GEO_CHECK_URL=https://your-geo-api.example.com/geo

